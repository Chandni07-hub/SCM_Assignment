name: Welcome new contributors

on: 
  pull_request_target:
    types: [opened]

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          pr-message: '👋 Welcome! Thank you for your first pull request. A maintainer will review it soon!'

